@if (ViewContext.ModelState.IsValid == false)
{
    <div class="alert alert-danger">
        <strong>Хуй там</strong>
        <div asp-validation-summary="ModelOnly" class="danger"></div>
    </div>
}